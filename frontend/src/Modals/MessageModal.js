class MessageModal {
  constructor(from = "", to = "", message = "", date_of_message = "") {
    this.from = from;
    this.to = to;
    this.message = message;
    this.date_of_message = date_of_message;
  }
}

export default MessageModal
